# IHearYou Firmware Partition Table — 8MB flash (ESP32-S3R8 on ReSpeaker Lite/XVF3800)
#
# OTA-ready two-slot layout, no factory partition: factory + 2 OTA slots cannot fit
# in 8MB at useful app sizes (the previous table needed 9.06MB and failed the build,
# while sdkconfig.defaults already declared FLASHSIZE_8MB). idf.py flash writes the
# app to ota_0; the bootloader boots it via otadata. Keeping the OTA slots NOW is
# deliberate: repartitioning a deployed node later would erase NVS and destroy its
# Wi-Fi/broker provisioning, so the field-update-capable layout must be on-silicon
# from the first flash. `storage` (spiffs) is reserved for future use (e.g. CA cert,
# audio prompts for the speaker path).
#
# Name,    Type, SubType, Offset,   Size,     Flags
nvs,       data, nvs,     0x9000,   0x6000,
otadata,   data, ota,     0xf000,   0x2000,
phy_init,  data, phy,     0x11000,  0x1000,
ota_0,     app,  ota_0,   0x20000,  0x300000,
ota_1,     app,  ota_1,   0x320000, 0x300000,
storage,   data, spiffs,  0x620000, 0x1D0000,
